for i = 1:numel(datastream)
    if ~isempty(datastream{i})
disp([num2str(i) ' & ' datastream{i}.instrument.name ...
    ' & Raw_' datastream{i}.instrument.variable ...
    ' & ' num2str(datastream{i}.config.height) '//'])
    end
end